<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Background color -->
    <corners android:radius="24dp" /> <!-- Rounded corners -->
    <stroke
        android:width="4dp"
        android:color="@color/green_sat_dark" /> <!-- Light green border -->
    <padding
        android:left="12dp"
        android:top="8dp"
        android:right="12dp"
        android:bottom="8dp" />
</shape>