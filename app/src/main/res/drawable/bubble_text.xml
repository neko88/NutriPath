<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Background color -->
    <corners android:radius="24dp" /> <!-- Rounded corners -->
    <stroke
        android:width="5dp"
        android:color="@color/green_med" /> <!-- Light green border -->
    <padding
        android:left="16dp"
        android:top="8dp"
        android:right="12dp"
        android:bottom="8dp" />

</shape>